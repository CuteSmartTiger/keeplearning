server {
    listen       8080;
    server_name  localhost default_server;

    location / {
        root   /var/www;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}
