conda create --name djangotest python=3.6

activate djangotest

pip install djangorestframework
pip install django
pip install markdown     
pip install django-filter

pip install django djangorestframework  markdown  django-filter
